<div
    {{ attributes
        .add(stimulus_controller('counter'))
    }}
>
    <button
        class="border-2 border-primary rounded-full w-16 h-16 flex items-center justify-center text-sm font-bold text-primary hover:cursor-pointer transition-all duration-200"
        aria-label="Mettre le chronomètre en pause"
        {{ stimulus_target('counter', 'countdown') }}>
    </button>
</div>
